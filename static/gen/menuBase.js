function openMenu(menu){menu.setAttribute("status","open");return}function closeMenu(menu){menu.setAttribute("status","closed");return}function toggleMenu(menuId){toggleMainMenu();var menu = document.getElementById(menuId);var status = menu.getAttribute("status");switch(status){case "open":closeMenu(menu);break;case "closed":openMenu(menu);break;default:closeMenu(menu)}return}$(".closeMenuIcon").click(function(event){var menuId = event.target.parentNode.getAttribute("id");var menu = document.getElementById(menuId);closeMenu(menu);toggleMainMenu()})